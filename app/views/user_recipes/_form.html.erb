<%= form_for(user_recipe) do |f| %>
  <% if user_recipe.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(user_recipe.errors.count, "error") %> prohibited this user_recipe from being saved:</h2>

      <ul>
      <% user_recipe.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :user_id %>
    <%= f.number_field :user_id %>
  </div>

  <div class="field">
    <%= f.label :recipe_id %>
    <%= f.number_field :recipe_id %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
