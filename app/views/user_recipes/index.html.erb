<p id="notice"><%= notice %></p>

<h1>User Recipes</h1>

<table>
  <thead>
    <tr>
      <th>User</th>
      <th>Recipe</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @user_recipes.each do |user_recipe| %>
      <tr>
        <td><%= user_recipe.user_id %></td>
        <td><%= user_recipe.recipe_id %></td>
        <td><%= link_to 'Show', user_recipe %></td>
        <td><%= link_to 'Edit', edit_user_recipe_path(user_recipe) %></td>
        <td><%= link_to 'Destroy', user_recipe, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New User Recipe', new_user_recipe_path %>
